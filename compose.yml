services:
  db:
    image: postgres:18
    expose:
      - 5432
    volumes:
      # Mount the local folder with your SQL files
      - ./db/migrations/init-scripts:/docker-entrypoint-initdb.d:ro
      # Persist the actual database data
      - postgres_data:/var/lib/postgresql/18/docker
volumes:
  postgres_data: