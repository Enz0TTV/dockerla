# Structure

- `./migrations` : Scripts d'initialisation / de seeding de la DB
- `./pgadmin` : Fichiers de configuration de pgadmin (actuellement, contient que `servers.json` pour initialiser les serveurs dans pgadmin)